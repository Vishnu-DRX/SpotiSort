---
description: Run the sorter in dry-run mode and summarise what it would do
---
Run `python src/sync.py` (dry-run, no `--apply`). Summarise: tracks evaluated, planned moves grouped
by target playlist, skipped (no match / missing playlist), warnings. Do not run with `--apply`.
