# Phase 6 report — Pages config builder (PWA)

## 1. Verdict: **PASS** (one criterion substituted, see §5)
1. Playwright builds each rule type — PASS (68 e2e tests).
2. YAML round-trips through the real `src.config` validator — PASS (downloaded/copied output goes through `parse_config`; JS validator compared with `parse_config` on 22 documents, messages included).
3. Installable PWA — PASS by manifest/icon/service-worker/offline tests; **Lighthouse check not run** (substituted).
4. Screenshots at 375 px and 1280 px — PASS: `docs/screenshots/builder-375.png`, `builder-1280.png`; no horizontal overflow at 375.
5. CI — PASS: `e2e` workflow green on `main` (run 35611562532, bundled Chromium); `tests` green (35611562385).

## 2. Proof
```
$ python -m pytest tests/e2e -q -m e2e     -> 75 passed (~25 s locally on Edge; CI on Playwright Chromium was green on the previous 68-test revision)
$ python -m pytest -q                       -> 706 passed, 2 skipped, 75 deselected
$ gh run list  -> completed success  e2e   35611562532 (56s)   completed success  Tests  35611562385 (20s)
```
Live site: https://vishnu-drx.github.io/SpotiSort/builder/ (Pages build success after the push).

## 3. What was built
- `docs/builder/` (index.html, app.js, builder.css, validate.js = JS port of `src/config.py`, languages.js generated by `scripts/gen_languages_js.py`),
  `docs/vendor/js-yaml.min.js` (4.1.0, MIT), `docs/manifest.webmanifest`, `docs/sw.js` (cache `spotisort-shell-v1`), `docs/icons/*` (`scripts/gen_icons.py`, stdlib only),
  landing page link, README paragraph, `tests/e2e/*`, `tests/test_languages_js_sync.py` (fails if generated JS alias table is stale), `.github/workflows/e2e.yml`.
- Features: all schema keys incl. `language_playlists`/`enrichment`, add/remove/reorder rules (keyboard), live YAML, Copy/Download blocked with an error summary when invalid,
  import by file/paste, alias normalisation (`hi` -> `hindi`), offline after first visit, dark/light, 375 px layout, no runtime external requests.

## 4. GitHub setup
Added workflow `e2e.yml`. `pytest.ini` now has `addopts = -m "not e2e"`; `requirements-dev.txt` gained playwright + pytest-playwright (dev only).

## 5. Deviations / Questions for master
1. Lighthouse installability was not run; replaced by manifest + icon + SW scope + offline-reload tests. Say if a real Lighthouse run is required.
2. Locally the agent could not download Playwright's Chromium (network timeouts) and ran on Edge; CI ran the same suite on bundled Chromium and passed.
3. Copy/Download use `aria-disabled` (still focusable) rather than `disabled`; one extra validation beyond `config.py`: duplicate playlist names in `language_playlists`.
4. Playwright/pytest-playwright are new **dev** dependencies (runtime stays stdlib + requests + PyYAML).

## 6. Risks & known gaps
- Bump `CACHE_VERSION` in `docs/sw.js` whenever cached shell files change, or users see stale assets.
- JS validator mirrors the schema frozen at Phase 2; any later schema change needs `gen_languages_js.py` + `validate.js` updates (the sync tests will catch drift).

## 7. Agents used
One agent built the PWA + e2e suite (long-running, ~20 min); I reviewed the 1280 px screenshot, the file list and CI results, ran the suites, and committed.

## 8. Next
Phase 7 (GitHub write-back) is gated behind Phase 4 by the spec. Nothing from Phase 6 blocks Phases 2-5.

## Update (after Phase 2 review)
Builder gained `enrichment.english_default` (checkbox, validator parity with `src/config.py`, YAML export/import); 7 new e2e tests (75 total, all pass locally);
service-worker cache bumped to `spotisort-shell-v2`. The 75-test revision has not yet been through CI at the time of writing (see final summary for the run).
Master review of this report is requested.
