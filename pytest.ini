[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not e2e"
markers =
    live: hits the real Spotify API; skipped unless SPOTISORT_LIVE=1
    e2e: browser tests for the Pages config builder (need playwright + a browser; run with -m e2e)
