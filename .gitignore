# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Secrets and local explore output — never commit
explore_output.json
*.env
.env

# Claude Code local overrides
.claude/settings.local.json

# Local enrichment cache (contains your library's artist ids); Phase 4 workflow will commit it with git add -f
.cache/enrichment.json
config.local.yaml

# Local dry-run/real run logs (dated); the Phase 4 workflow commits them with git add -f
logs/2*.json
